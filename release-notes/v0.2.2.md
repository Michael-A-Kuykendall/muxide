## Highlights

- Correctness fixes: AV1 keyframe detection, finalized-state checks in `write_video()`, and fragmented `moov` box ordering
- CLI improvements: `info` now detects nested codec FourCCs correctly and `--creation-time` now applies metadata instead of warning
- API cleanup: `MuxerConfig::into_builder()` added, path arguments normalized to `&Path`, and metadata construction unified in the CLI
- Dead code removed: deleted the unreachable `src/config.rs`, duplicate builder methods, and `Muxer::flush()`

Full Changelog: https://github.com/Michael-A-Kuykendall/muxide/compare/v0.2.1...v0.2.2v0.2.2